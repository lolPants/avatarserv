{
  "name": "avatar-serv",
  "packageManager": "yarn@3.3.1",
  "license": "MIT",
  "private": true,
  "scripts": {
    "check": "tsc --noEmit --skipLibCheck",
    "lint": "prettier --check . && eslint src --ext ts",
    "format": "prettier --write . && eslint src --ext ts --fix",
    "fmt": "yarn run format",
    "test": "yarn run check && yarn run lint"
  },
  "devDependencies": {
    "@lolpants/eslint-config": "^0.3.0",
    "@types/node": "^18.11.18",
    "@vercel/node": "^2.8.13",
    "eslint": "^8.32.0",
    "prettier": "^2.8.3"
  },
  "dependencies": {
    "axios": "^1.2.3",
    "next-connect": "^0.13.0"
  }
}
